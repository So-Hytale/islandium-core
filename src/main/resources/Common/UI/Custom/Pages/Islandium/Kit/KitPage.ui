$C = "../../../Common.ui";

$C.@PageOverlay {
    $C.@Container {
        Anchor: (Width: 900, Height: 560);

        #Title {
            Group {
                $C.@Title {
                    @Text = "KITS";
                }
            }
        }

        #Content {
            LayoutMode: Top;

            // Header bar
            Group #HeaderBar {
                Anchor: (Height: 50);
                Background: (Color: #16212f);
                LayoutMode: Left;
                Padding: (Horizontal: 20);

                Label #HeaderTitle {
                    FlexWeight: 1;
                    Text: "KITS";
                    Style: (FontSize: 16, TextColor: #4fc3f7, RenderBold: true, RenderUppercase: true, VerticalAlignment: Center);
                }

                TextButton #BackBtn {
                    Anchor: (Width: 80, Height: 32);
                    Visible: false;
                    Text: "< Retour";
                    Style: TextButtonStyle(
                        Default: (
                            LabelStyle: (FontSize: 12, TextColor: #96a9be, VerticalAlignment: Center)
                        ),
                        Hovered: (
                            Background: #1a2836,
                            LabelStyle: (FontSize: 12, TextColor: #ffffff, VerticalAlignment: Center)
                        ),
                        Sounds: $C.@ButtonSounds,
                    );
                }

                TextButton #CloseButton {
                    Anchor: (Width: 40, Height: 36);
                    Text: "X";
                    Style: TextButtonStyle(
                        Default: (Background: #2a1a1a, LabelStyle: (FontSize: 16, TextColor: #ff4444, HorizontalAlignment: Center, VerticalAlignment: Center, RenderBold: true)),
                        Hovered: (Background: #3a2a2a, LabelStyle: (FontSize: 16, TextColor: #ff6666, HorizontalAlignment: Center, VerticalAlignment: Center, RenderBold: true)),
                        Sounds: $C.@ButtonSounds
                    );
                }
            }

            // Dynamic content area (pour le detail d'un kit)
            Group #PageContent {
                FlexWeight: 1;
                LayoutMode: TopScrolling;
                ScrollbarStyle: $C.@DefaultScrollbarStyle;
                Padding: (Full: 20);
                Visible: false;
            }

            // Hub Grid - grille dynamique des kits (generee en Java)
            Group #HubGrid {
                FlexWeight: 1;
                LayoutMode: TopScrolling;
                ScrollbarStyle: $C.@DefaultScrollbarStyle;
                Padding: (Full: 25);
            }
        }
    }
}
