$C = "../../Common.ui";

@DefaultIcon = PatchStyle(TexturePath: "Pages/Islandium/Icons/default.png");

$C.@PageOverlay {
    $C.@Container {
        Anchor: (Width: 1050, Height: 650);

        #Title {
            Group {
                $C.@Title {
                    @Text = "ISLANDIUM";
                }
            }
        }

        #Content {
            LayoutMode: Top;

            // Header bar
            Group #HeaderBar {
                Anchor: (Height: 50);
                Background: (Color: #16212f);
                LayoutMode: Left;
                Padding: (Horizontal: 20);

                Label {
                    FlexWeight: 1;
                    Text: "Menu Principal";
                    Style: (FontSize: 16, TextColor: #96a9be, VerticalAlignment: Center);
                }

                TextButton #CloseButton {
                    Anchor: (Width: 40, Height: 40);
                    Text: "X";
                    Style: TextButtonStyle(
                        Default: (
                            LabelStyle: (FontSize: 16, TextColor: #ff4444, HorizontalAlignment: Center, VerticalAlignment: Center, RenderBold: true)
                        ),
                        Hovered: (
                            Background: #2a1a1a,
                            LabelStyle: (FontSize: 16, TextColor: #ff6666, HorizontalAlignment: Center, VerticalAlignment: Center, RenderBold: true)
                        ),
                        Sounds: $C.@ButtonSounds,
                    );
                }
            }

            // Card grid area - grille de cartes style Prison
            Group #CardGrid {
                FlexWeight: 1;
                LayoutMode: TopScrolling;
                ScrollbarStyle: $C.@DefaultScrollbarStyle;
                Padding: (Full: 25);
            }
        }
    }
}
